use studentworks;
--  you can use another table name
create table addressbook(
    _id varchar(10) primary key not null,
    _name varchar(10),
    _city varchar(50),
    _state varchar(50),
    _phone varchar(13)
    _email varchar(200) 
);
